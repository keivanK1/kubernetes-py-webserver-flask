apiVersion: v1
kind: Service
metadata:
  name: simple-webserver-service
  labels: 
    name: simple-webserver-service
    app: simple-webserver-app
spec:
  type: LoadBalancer
  ports:
  - port:  80
    targetPort: 80
  selector:
    name: simple-webserver-pod
    app: simple-webserver-app